export interface account {
    id: number,
    accountNumber: string,
    accountType: number,
    balance: number,
    userId: number,
    status: number,
    createdAt: string,
    updatedAt: string
}